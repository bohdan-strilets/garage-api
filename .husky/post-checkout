# auto-install if deps changed
git diff --name-only HEAD@{1} HEAD | grep -E "(^|/)(package\.json|yarn\.lock)$" >/dev/null 2>&1 && \
  (echo "📦 Deps changed — running yarn install..." && yarn install --frozen-lockfile || yarn install)
